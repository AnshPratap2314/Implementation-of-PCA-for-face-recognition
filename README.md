# Implementation-of-PCA-for-face-recognition
Face recognition system using PCA and ANN. PCA extracts key facial features (eigenfaces) for dimensionality reduction, and an MLP classifier performs classification. Includes preprocessing, accuracy optimization using different k values, and imposter detection using probability thresholding.
